import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(alignment: Alignment.center,color: Colors.blueAccent, child: Text("Food", textDirection: TextDirection.ltr,),
    // height: 96.0,
    // width: 192.0,
    // margin: EdgeInsets.only(left: 50.0),
    ));
  }
  
}